# Root requirements file consumed by the Vercel Python runtime for the api/ function.
# Kept intentionally minimal: only the packages the deployed API actually imports.
# The full development snapshot lives in backend/requirements.txt and is not used here.

fastapi==0.110.1
starlette==0.37.2
pydantic==2.13.5
pydantic_core==2.46.5
anyio==4.15.1

# MongoDB driver. dnspython is required for mongodb+srv:// connection strings (Atlas).
motor==3.3.1
pymongo==4.6.3
dnspython==2.8.0
certifi==2026.7.22

# Admin password hashing.
bcrypt==4.1.3

# Invoice and brochure PDF rendering.
reportlab==5.0.1
pillow==12.3.0

# Brochure image fetching (falls back to the committed cache when offline).
httpx==0.28.1

# server.py loads a local .env during import. Harmless on Vercel (no file present).
python-dotenv==1.2.3
